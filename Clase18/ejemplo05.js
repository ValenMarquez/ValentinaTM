const colores = ['uno', 'dos', 'tres'];

const [rojo, amarillo, verde] = colores;

const nombre= 'Flavia'

console.log(rojo);

//la destructuración es una tecnica para declarar y extraer varias variables a la vez